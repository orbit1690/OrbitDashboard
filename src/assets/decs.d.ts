// In order to import an image in TypeScript, we need to declare it as a module.
// Here we declare every file with .png extension as a module so, we could import them in TypeScript.
declare module "*.png";
